classDiagram
    class Loan {
        - id
        - book
        - user
        - dueData
        - retriveDate
        - loanDate
        + loan(book, user, dueData, retriveData, loanData)
        + renew(newDuaDate)
        + return()
        + isOverDue()
    }
    class Notification {
        <<interface>>
        + notify(loan)
    }

    class EmailNotifier {
        + notify(loan)
    }


    Loan --> Notification : usa
    EmailNotifier ..|> Notification : implementa